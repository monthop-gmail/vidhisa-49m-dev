services:
  vithisa-dashboard:
    image: nginx:alpine
    ports:
      - "${DASHBOARD_PORT:-8080}:80"
    volumes:
      - ./html:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - vithisa-api
    networks:
      - vithisa-net
