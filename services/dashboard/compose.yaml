services:
  vidhisa-dashboard:
    build:
      context: ..
      dockerfile: dashboard/Dockerfile
    ports:
      - "${DASHBOARD_PORT:-8080}:80"
    depends_on:
      - vidhisa-api
    networks:
      - vidhisa-net
