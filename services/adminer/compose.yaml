services:
  vidhisa-adminer:
    image: adminer:latest
    ports:
      - "${ADMINER_PORT:-8081}:8080"
    environment:
      ADMINER_DEFAULT_SERVER: vidhisa-db
      ADMINER_DESIGN: nette
    depends_on:
      - vidhisa-db
    networks:
      - vidhisa-net
