services:
  vithisa-adminer:
    image: adminer:latest
    ports:
      - "${ADMINER_PORT:-8081}:8080"
    environment:
      ADMINER_DEFAULT_SERVER: vithisa-db
      ADMINER_DESIGN: nette
    depends_on:
      - vithisa-db
    networks:
      - vithisa-net
